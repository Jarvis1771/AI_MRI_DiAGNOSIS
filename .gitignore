# ── Runtime artifacts ──
*.pdf
uploaded_scan.png
uploaded_image.png
gradcam_result.png
heatmap.png

# ── Dataset (too large to commit) ──
data/
dataset/
*.zip

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.egg-info/

# ── macOS ──
.DS_Store

# ── Gradio cache (legacy) ──
.gradio/
flagged/

# ── Virtual environments ──
venv/
.venv/
env/

# ── IDE ──
.idea/
.vscode/
*.swp

# ── Static uploads (runtime) ──
static/uploads/

# ── Model weights (upload separately or use Git LFS) ──
# Uncomment the line below if NOT using Git LFS:
# models/*.pth
